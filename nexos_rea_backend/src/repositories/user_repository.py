# User repository retired as of Sprint A.
# User model (with password_hash) removed; auth is Supabase's responsibility.
# User profiles are queried via src/repositories/perfil_repository.py (Profile model).
